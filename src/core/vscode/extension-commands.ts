import { EXTENSION_CONFIG_PREFIX } from '../../constants';

export const ExtensionCommands = {
  ShowLog: `${EXTENSION_CONFIG_PREFIX}.showLog`,
  Reload: `${EXTENSION_CONFIG_PREFIX}.reload`,
  Reset: `${EXTENSION_CONFIG_PREFIX}.reset`,
  ExecuteFunction: `${EXTENSION_CONFIG_PREFIX}.executeFunction`
};
